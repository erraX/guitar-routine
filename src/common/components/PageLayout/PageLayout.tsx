import styled from 'styled-components';
import { Card } from '@common/components/Card';

export const PageLayout = styled(Card)`
  width: 100%;
  padding: 50px;
  display: flex;
  margin-bottom: 20px;
`;
